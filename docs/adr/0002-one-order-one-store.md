---
status: accepted
---

# Keep each order within one store

Every order belongs to exactly one store. The platform can serve multiple stores, but a single order cannot combine products from different stores; this keeps order, payment, settlement, and delivery lifecycles independently consistent without a cross-store transaction.
