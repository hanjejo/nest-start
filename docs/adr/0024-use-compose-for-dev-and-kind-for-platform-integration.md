---
status: accepted
---

# Use Compose for development and kind for platform integration

Docker Compose supports fast local application development, while `kind` runs the full Kubernetes integration stack with Argo CD, RabbitMQ, Redis, and LGTM. Terraform AWS follows after the application specification is stable, so cloud infrastructure does not block domain and application learning.
